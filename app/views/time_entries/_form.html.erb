
<%= form_for(@time_entry) do |f| %>
  <% if @time_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@time_entry.errors.count, "error") %> prohibited this time_entry from being saved:</h2>

      <ul>
      <% @time_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.select :project_id, options_from_collection_for_select(@projects, 'id', 'name') , class: 'form-group'%>
    
  </div>
  <div class="field">
    <%= f.label :task_id %><br>
    <%= f.select :task_id, options_from_collection_for_select(@tasks, 'id', 'name') , class: 'form-group'%>
  </div>
  
  <div class="field">
    <%= f.label :hours %><br>
    <%= f.number_field :hours , class: 'form-group'%>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date , class: 'form-group'%>
  </div>
  <div class="actions">
    <%= f.submit "Submit", class: "btn btn-primary"%>
  </div>
<% end %>


