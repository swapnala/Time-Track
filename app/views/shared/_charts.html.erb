<div class="container-fluid">

<h4>Billable(t) Vs Non Billable(f) Tasks</h4>
<%= pie_chart TimeEntry.joins(:task).includes(:task).group(:'tasks.billable').count %>
<h4>Time Spent as per Projects</h4>
<%= pie_chart TimeEntry.joins(:project).includes(:project).group(:'projects.name').count %>
<h4>Time Spent as per Tasks<h4>
<%= pie_chart TimeEntry.joins(:task).includes(:task).group(:'tasks.name').count %>
</div>
